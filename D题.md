# 2026 年高教社杯全国大学生数学建模竞赛题目

（请先阅读"全国大学生数学建模竞赛论文格式规范"）

## D 题 时频冲突检测与消解

随着用频装备数量剧增，电磁兼容矛盾日益突出。在制定用频计划时，为避免用频装备之间相互干扰和制约，需要对其中潜在的时频冲突进行检测和消解。

现考虑某特定区域内，大量用频装备在一段时间内对某一有限带宽的频率资源都有使用需求的情形。为简化问题，对时频资源进行离散化处理：

- **时域**：用固定的时间长度 $\Delta t$ 作为时间资源的基本单位，表示用频时间不可拆分的最小值，规定用频计划所占用的时间长度必须是 $\Delta t$ 的整数倍；
- **频域**：以固定的频带宽度 $\Delta f$ 将系统可提供的有限带宽频率资源离散化为 **100 个**宽度相同的频段，每一个用频计划可以占用连续的多个频段，并且占用的频段数量保持不变。

所有用频装备均提报了用频计划，需从提报的用频计划中检测潜在的时频冲突。每个用频计划包含四个参数：

| 参数 | 说明 |
|------|------|
| 频段区间 | 占用的连续频段范围 |
| 首次使用的时间区间 | 首次使用该频段的时间范围 |
| 间隔时长 | 相邻两次使用该频段的间隔时间 |
| 使用次数 | 按规则重复使用的次数 |

当两个用频计划的**时间区间**和**频段区间**都存在交叠时，则判定它们之间存在时频冲突。

时频冲突消解就是通过调整某些用频计划的参数（比如平移频段区间或者时间区间等），甚至撤销某些用频计划，使得消解后的用频计划不存在时频冲突。一个用频计划只能调整其中一个参数或撤销。

### 消解原则

1. 除非特别说明，一般**不调整使用次数和使用间隔时长**；
2. 应尽可能**减少撤销**用频计划；
3. 尽量**减少被调整的用频计划数量**；
4. 按装备类别区分优先级：**A 类 > B 类 > C 类**，高优先级用频装备的用频计划应尽量保持；
5. 尽量**减少调整的幅度**，规定频段、时间调整的最大平移幅度分别为 $10\Delta f$、$5\Delta t$。

---

## 附件 1 给出了 3 类共 150 个用频装备提报的用频计划，建立数学模型解决以下问题。

### 问题 1

对附件 1 给出的用频计划进行**时频冲突检测**，将结果保存到文件 `result1.xlsx` 中（模板文件见附件 2），并在论文中给出时频冲突的统计结果。

### 问题 2

对问题 1 中检测出的时频冲突进行**消解**，将消解方案保存到文件 `result2.xlsx` 中（模板文件见附件 2），并在论文中按表 1 的格式给出时频冲突消解的统计结果。

**表 1 时频冲突消解的统计结果**

| 类别 | 保留数量 | 调整数量 | 撤销数量 |
|------|----------|----------|----------|
| A 类 |          |          |          |
| B 类 |          |          |          |
| C 类 |          |          |          |

### 问题 3

基于问题 2 求解得到的无冲突用频计划，如果**不限制频段和时间的平移幅度**，在不增加时频资源的前提下，**最多还能安排多少 C 类用频装备**？给出相应的用频计划，将结果保存到文件 `result3.xlsx` 中（模板文件见附件 2）。

### 问题 4

若允许部分 C 类用频装备对**用频间隔时长**进行调整，但要求与原间隔时长的差异不超过 $10\Delta t$，请对问题 1 中检测出的时频冲突进行消解，将消解方案保存到文件 `result4.xlsx` 中（模板文件见附件 2），并在论文中按表 1 的格式给出时频冲突消解的统计结果。

---

## 附件说明

### 附件 1 — 用频装备的用频计划

以用频装备 **A001** 为例：

- 频段区间 $[80, 90)$：表示占用的频段区间为 $[f_0 + 80\Delta f,\ f_0 + 90\Delta f)$，其中 $f_0$ 是该有限带宽频段资源的下限；
- 时间区间 $[35, 40)$：表示该装备用频计划首次使用该频段的时间区间为 $[35\Delta t,\ 40\Delta t)$；
- 间隔时长 **60**：表示相隔 $60\Delta t$ 的时间再次以相同长度的时间区间占用该频段，即第二次占用频段 $[f_0 + 80\Delta f,\ f_0 + 90\Delta f)$ 的时间区间为 $[40\Delta t + 60\Delta t,\ 45\Delta t + 60\Delta t)$；
- 使用次数 **3**：表示该用频装备按上述规则使用 3 次该频段。

### 附件 2 — 结果模板文件

| 文件 | 说明 |
|------|------|
| `result1.xlsx` | 问题 1 结果模板。每行填写一对用频计划冲突：A 列填写序号，B 列和 C 列分别填写冲突的两个用频装备编号。 |
| `result2.xlsx` | 问题 2 结果模板。需调整的用频装备：若调整频段，A 列填装备编号、B 列填调整后频段范围；若调整时间区间，A 列填装备编号、C 列填调整后时间区间；若撤销，A 列填装备编号、D 列填"是"。 |
| `result3.xlsx` | 问题 3 结果模板。新增的 C 类用频装备：A 列填序号，B 列和 C 列填入频段区间和时间区间。 |
| `result4.xlsx` | 问题 4 结果模板。需调整的装备：A 列填装备编号，若调整频段填 B 列，若调整时间区间填 C 列，若调整时间间隔填 D 列，若撤销填 E 列"是"。 |
